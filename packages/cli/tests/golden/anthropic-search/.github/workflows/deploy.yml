# placeholder: Task 3.3 fills this in.
# The deploy workflow is intentionally a stub here so the scaffold remains
# self-contained and the file path is reserved. Task 3.3 (parallel) lands
# the real template covering: build → upload-artifacts → versions-upload →
# start-run. Do NOT add `wrangler deploy` here — promotion is the
# orchestrator's job.

name: deploy
on:
  push:
    branches: [main]
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "placeholder: replaced by Task 3.3"
