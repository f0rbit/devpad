/**
 * Ambient declaration for `@devpad/pipeline-templates`.
 *
 * The package is workspace-only (not published to npm) and is consumed by
 * the orchestrator at deploy time from the source file in this repo. We
 * declare a minimal surface here so `tsc --noEmit` is satisfied without
 * vendoring the full DSL.
 */

declare module "@devpad/pipeline-templates" {
	export type DslError = { code: string; message: string };
	export type ExtendTemplateOverrides = Record<string, unknown>;
	export const manual: (opts?: { reason?: string }) => unknown;
	export const auto: (opts?: { afterBake?: boolean }) => unknown;
	export const analysis: (ref: { template_id: string }) => unknown;
	export const extendTemplate: (overrides: ExtendTemplateOverrides) => {
		ok: boolean;
		value?: unknown;
		error?: DslError;
	};
}
