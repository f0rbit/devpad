import { DEBUG_LOGGING, log } from "../../../../tests/shared/test-utils";

export const unit_test_config = {
	// Configuration for unit testing individual functions
	// Unit tests should not require external dependencies
};

export { DEBUG_LOGGING, log };
