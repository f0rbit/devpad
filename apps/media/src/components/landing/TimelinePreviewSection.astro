---
import TimelinePreview from "./TimelinePreview";
---

<section id="preview" class="landing-section">
	<h2 class="section-title">See it in action</h2>
	<p class="text-muted" style="text-align: center; margin: 0;">A unified view of your commits, posts, and thoughts.</p>
	<div class="timeline-demo-container">
		<TimelinePreview client:load />
	</div>
</section>

<style>
	.landing-section {
		max-width: 1000px;
		margin: 0 auto;
		padding: calc(var(--space-xl) * 1.5) var(--space-md);
		width: 100%;
		box-sizing: border-box;
		display: flex;
		flex-direction: column;
		gap: var(--space-lg);
	}

	.section-title {
		text-align: center;
		margin: 0;
	}

	.timeline-demo-container {
		border: 1px solid var(--border);
		border-radius: 4px;
		padding-left: 10px;
		padding-top: 4px;
		padding-bottom: 4px;
	}
</style>
